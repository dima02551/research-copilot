# Leave unset to run in demo mode (deterministic fake research, no API cost).
ANTHROPIC_API_KEY=

# Comma-separated list of allowed frontend origins.
CORS_ORIGINS=http://localhost:5173,http://localhost:5183

DATABASE_PATH=research.db
